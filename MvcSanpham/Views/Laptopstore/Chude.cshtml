@model IEnumerable<MvcSanpham.Models.CHUDE>
<div class="category-nav">
    <span class="category-header">Categories <i class="fa fa-list"></i></span>
    <ul class="category-list">
        @foreach (var item in Model)
        {
            <a href="@Url.Action("Sptheochdue", "Laptopstore", new {id = item.MaCD})" class="list-group-item">

            
                @item.TenChuDe
            </a>
        }
    </ul>
</div>
